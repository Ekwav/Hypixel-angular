import { PlayerAuctionsResponse } from './player-auctions-response';

describe('PlayerAuctionsResponse', () => {
  it('should create an instance', () => {
    expect(new PlayerAuctionsResponse()).toBeTruthy();
  });
});
